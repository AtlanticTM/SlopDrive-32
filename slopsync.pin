a27b122e75943b60ca5f5e9446b6849094fe84e1
# The SlopSync repo (sibling checkout ../SlopSync) commit this machine repo
# builds against. tools/canon_lint.py's pin rule FAILs if ../SlopSync is
# missing or its HEAD doesn't match this line, and cross-checks the frozen
# conformance artifacts (mini_catalog.hpp / mini-catalog.yaml) in the
# sibling against the same sha256 pins SlopSync's own slopsync_lint.py
# carries — belt + suspenders across the repo boundary. Bump this line (and
# re-verify the whole gauntlet) whenever the sibling advances; never edit it
# to make a check pass without actually re-pointing at a real, tested sha.
